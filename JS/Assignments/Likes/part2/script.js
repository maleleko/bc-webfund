//  working code

var num = 9; {}
var numClass = document.querySelector(".numlike");

function addlike() {
    num++;
    numClass.innerText = num + " like(s)"
}

var count = 12;
var countClass = document.querySelector(".add");

function addup() {
    count++;
    countClass.innerText = count + " like(s)"
}

var plus = 9;
var plusClass = document.querySelector(".plus");

function  plusup() {
    plus++;
    plusClass.innerText = plus + " like(s)"
}